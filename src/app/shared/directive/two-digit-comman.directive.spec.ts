import { TwoDigitCommanDirective } from './two-digit-comman.directive';

describe('TwoDigitCommanDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoDigitCommanDirective();
    expect(directive).toBeTruthy();
  });
});
